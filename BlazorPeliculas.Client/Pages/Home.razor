@page "/"
@page "/index"

@attribute [StreamRendering]
@inject IServicioPeliculas servicioPeliculas

<PageTitle>Home</PageTitle>

@* como no necesitamos interactividad con este componente, tiene sentido dejarlo como servidor estatico*@

@* con el @ref se hace referencia a un componente para poder manipularlo facilmente *@
<h3>En Cartelera</h3>
<ListadoPeliculas Peliculas="_homeDTO.EnCartelera"/>

<h3>Futuros Estrenos</h3>
<ListadoPeliculas Peliculas="_homeDTO.FuturosEstrenos"/>    


@code {
    private HomeDTO _homeDTO = new();

    //como no va a tener ningun tipo de interactividad, no es necesario usar el OnInitializedAsync, asi que se comenta
    // protected override async Task OnAfterRenderAsync(bool firstRender)
    // {
    //     if(!firstRender)
    //     {
    //         return;
    //     }

    //     await Task.Yield();

    //     _homeDTO = await servicioPeliculas.ObtenerPeliculasHome();
    //     StateHasChanged();
    // }

    protected override async Task OnInitializedAsync()
    {
        _homeDTO = await servicioPeliculas.ObtenerPeliculasHome();
    }
}